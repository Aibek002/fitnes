<?php
// Проверяем, был ли отправлен POST-запрос с параметром day
if (isset($_POST['day'])) {
    // Получаем значение параметра day
    $day = $_POST['day'];

    // Ваш код для получения плана еды на выбранный день
    // Например, извлекаем данные из базы данных
    // $mealPlan = ваша_функция_для_получения_плана_еды($day);

    // Примерный код для получения плана еды
    $mealPlan = "<h3>План еды на $day число:</h3>
                 <ul>
                     <li>Завтрак: Айбек</li>
                     <li>Обед: ...</li>
                     <li>Ужин: ...</li>
                 </ul>";

    // Возвращаем план еды в качестве ответа на AJAX-запрос
    echo $mealPlan;
} else {
    // Если параметр day не был отправлен, возвращаем пустой ответ или сообщение об ошибке
    echo 'Ошибка: параметр day не был отправлен.';
}
?>
